{% extends "base.html.twig" %}

{% block body %}
<header>
	<div class="container-fluid">
		<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
			<div class="container-fluid">
				<a class="navbar-brand" href="#">Luxeloc</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNavDropdown">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="#">Home</a>
						</li>
						<li class="nav-item">
                <a class="nav-link" href="{{ path('liste_vehicules') }}">Vehicules</a>


						
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a>
                            </li>
                        {# si l'utilisateur est authentifié et a le rôle ADMIN #}
                        {% if is_granted('IS_AUTHENTIFICATED_FULLY') and is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
								Admin
							</a>
							<ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
								<li>
									<a class="dropdown-item" href="{{ path('admin_ajout_vehicule') }}"> ajout vehicule</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('admin_app_vehicules') }}"> gestion vehicules</a>
								</li>
								<li>
									<a class="dropdown-item" href="#">Something else here</a>
								</li>
							</ul>
						</li>
                        {% endif %}
					</ul>
				</div>
                <p class="text-white">Bonjour Philippe Ambassa</p>
			</div>
		</nav>
	</div>
</header>

<main class="container mb-5">

{% for message in app.flashes('success') %}
<div class="alert alert-success text-center">
<p>{{ message}}</p>
  </div>    
{% endfor %}

{% for message in app.flashes('error') %}
	<div class="alert alert-success text-center">
		<p>{{ message}}</p>
	</div>
{% endfor %}


{% block content %}
                {# block qui reste vide ici. C'est la que le contenu propre à chaque page s'insèrera  #}

        {% endblock %}
        
        {% block final_js %}
        
        {% endblock %}
    </main>

    <footer class=" fixed-bottom bg-light"
		<div class="text-center">
			<p>
				&copy; All rights reserved, LuxeLoc
			</p>
		</div>
	</footer>

{% endblock %}